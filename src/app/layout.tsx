// Root layout — minimal shell. The [locale]/layout.tsx handles html, body, fonts, and i18n.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
